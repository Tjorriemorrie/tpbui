imports:
    - { resource: config.yml }

framework:
    router:   { resource: "%kernel.root_dir%/config/routing_dev.yml" }
    profiler: { only_exceptions: false }

web_profiler:
    toolbar: true
    intercept_redirects: false

monolog:
    handlers:
        main:
            type:  stream
            path:  %kernel.logs_dir%/%kernel.environment%.log
            level: debug
        firephp:
            type:  firephp
            level: info
#        swift:
#            type:       swift_mailer
#            from_email: system@afrihost.com
#            to_email:   dev@afrihost.com
#            subject:    An Error Occured!
#            level:      debug

assetic:
    use_controller: true
